{ 
  "name": "stockfish-nodejs",
  "version": "1.0.0",
  "description": "Chess analysis with Stockfish on AWS Lambda",
  "main": "index.js",
  "scripts": {
    "test": "vitest",
    "coverage": "vitest run --coverage"
  },
  "dependencies": {
    "chess.js": "^1.2.0"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.0.6",
    "c8": "^10.1.3",
    "vitest": "^4.0.6"
  }
}
